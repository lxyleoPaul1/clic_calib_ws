knot_interval_s: 0.05

# §4.5 R_smooth
alpha_p: 0.01
alpha_R: 0.01

# §4.7 robust kernels
lidar_cauchy_scale: 1.0
camera_huber_delta_px: 2.0

# Observation noise whitening: config/noise_model.yaml

# Hard clamp on |t_d| [s] (intersected with spline-support bounds).
t_d_max_abs_s: 0.1
